{
  "name": "pool-app",
  "version": "1.0.0",
  "description": "NFL Pool Application for Lagaan",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start-server": "cd backend && yarn start-server",
    "start-front": "cd frontend && yarn start",
    "start": "concurrently \"yarn start-server\" \"yarn start-front\" --kill-others --kill-others-on-fail"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "@auth0/auth0-react": "^1.0.0"
  }
}
